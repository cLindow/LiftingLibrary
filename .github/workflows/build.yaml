name: Build application

on: push

jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest
    steps:
      - name: Print Current Directory
        run: pwd
      - name: List Filed in Directory
        run: ls 
      
  
